import {createActionName} from "../../utility"

// navigation name
export const reducerName = "navigation"

// navigation actions type
export const SET_NAVIGATION = createActionName(reducerName, "SET_NAVIGATION")
